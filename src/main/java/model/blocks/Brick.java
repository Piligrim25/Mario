package model.blocks;

import javafx.scene.image.Image;

public class Brick extends Block {

	public Brick(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
