package model.blocks;

import javafx.scene.image.Image;

public class Stone extends Block {

	public Stone(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
