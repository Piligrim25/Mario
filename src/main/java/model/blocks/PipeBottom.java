package model.blocks;

import javafx.scene.image.Image;

public class PipeBottom extends Block {

	public PipeBottom(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
