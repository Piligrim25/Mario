package model.blocks;

import javafx.scene.image.Image;

public class PipeTop extends Block {

	public PipeTop(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
