package model.blocks;

import javafx.scene.image.Image;

public class Bonus extends Block {

	public Bonus(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
