package model.blocks;

import javafx.scene.image.Image;

public class Platform extends Block {

	public Platform(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
