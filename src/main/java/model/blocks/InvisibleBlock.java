package model.blocks;

import javafx.scene.image.Image;

public class InvisibleBlock extends Block {

	public InvisibleBlock(int x, int y, int blockSizeWidth, int blockSizeHeight, Image blocksImg) {
		super(x, y, blockSizeWidth, blockSizeHeight, blocksImg);
	}
}
